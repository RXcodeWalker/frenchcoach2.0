node_modules/
.env
dist/
.DS_Store
*.local

.vercel

backend/

french2.0/
.VSCodeCounter/

# S3 STT: teacher recordings + TN-derived material — never redistributed, never pooled.
data/sessions/

# Toolkit-generated reports (inspectAttempt HTML/terminal output, batch reports) — regenerable, not source.
data/reports/
