services:
  - type: web
    name: french-scoring-a11-stub
    env: node
    plan: free
    buildCommand: npm ci && npm run build:server
    startCommand: npm run start:server
    healthCheckPath: /health
