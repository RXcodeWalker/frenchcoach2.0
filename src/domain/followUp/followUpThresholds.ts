/**
 * Thresholds for the follow-up turn (Learn Mode, Phase 3 Slice D).
 * Product decision, not a measurement: caps session length, matching
 * practiceThresholds.ts's PRACTICE_MAX_PER_SESSION convention.
 */

/** Product decision, not a measurement: at most 3 follow-up turns per session. */
export const FOLLOWUP_MAX_PER_SESSION = 3;
